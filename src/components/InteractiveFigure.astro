---
import { Image, type ImageMetadata } from "astro:assets";

import type { Language } from "../i18n";
import { getTranslations } from "../i18n";

interface Props {
	lang: Language;
	src: ImageMetadata;
	alt: string;
	caption?: string;
}

const {
	lang,
	src,
	alt,
	caption,
} = Astro.props;

const labels = getTranslations(lang).interactiveFigure;

const dialogId = `figure-dialog-${src.src
	.replace(/[^a-zA-Z0-9]/g, "-")
	.replace(/-+/g, "-")}`;

const captionId = `${dialogId}-caption`;
---
<figure class="interactive-figure">
	<button
		type="button"
		class="interactive-figure-button"
		aria-label={`${labels.openLargerView}: ${alt}`}
		aria-haspopup="dialog"
		data-dialog-open={dialogId}
	>
		<Image
			src={src}
			alt={alt}
			class="interactive-figure-image"
		/>
	</button>

	{caption && (
		<figcaption class="interactive-figure-caption">
			{caption}
		</figcaption>
	)}
</figure>

<dialog
	id={dialogId}
	class="interactive-figure-dialog"
	aria-label={`${labels.largerView}: ${alt}`}
	aria-describedby={caption ? captionId : undefined}
>
	<div class="interactive-figure-dialog-content">
		<button
			type="button"
			class="interactive-figure-dialog-close"
			aria-label={labels.closePreview}
			data-dialog-close
		>
			×
		</button>

		<Image
			src={src}
			alt={alt}
			class="interactive-figure-dialog-image"
		/>

		{caption && (
			<p
				id={captionId}
				class="interactive-figure-dialog-caption"
			>
				{caption}
			</p>
		)}
	</div>
</dialog>

<script>
	import "../scripts/image-lightbox";
</script>