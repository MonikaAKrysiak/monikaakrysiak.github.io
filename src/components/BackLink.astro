---
interface Props {
	href?: string;
	label?: string;
}

const {
	href = "/#work",
	label = "Back to selected work",
} = Astro.props;
---

<a class="case-study-back-link" href={href}>
	<span aria-hidden="true">←</span>
	<span>{label}</span>
</a>